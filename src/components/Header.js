const Header = () => {

    return (
        <div>
            <header>
                <nav>
                    Test
                </nav>
            </header>
        </div>
    );
};

export default Header;
