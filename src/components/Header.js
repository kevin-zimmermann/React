const Header = () => {

    return (
        <div>
            <header>
                <nav className={"text-sky-500 text-center"}>
                    Test
                </nav>
            </header>
        </div>
    );
};

export default Header;
